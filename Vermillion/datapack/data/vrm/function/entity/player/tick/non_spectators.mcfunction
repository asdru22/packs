# Functions to run off non-spectator players every single tick

## Commands for holding items
execute if predicate vrm:entity_properties/slots/weapon/anything run function vrm:entity/player/tick/holding_item
