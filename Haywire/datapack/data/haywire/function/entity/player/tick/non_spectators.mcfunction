# Functions to run off non-spectator players every single tick

## Commands for holding items
execute if predicate haywire:entity_properties/slots/weapon/anything run function haywire:entity/player/tick/holding_item
